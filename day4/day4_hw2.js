function frequency()
{
    for(let i=0; i<=word.length-1; i++)//i=0,1,2,3,4,5
{
    for(let j=i+1;j<=word.length-1;j++ )
        {
            if ((word[i])===(word[j]))
            {
             i === i+1;
             console.log(word[i]);

          /*      let numberofsequence = "";
                numberofsequence = console.log((word[i]));
                let length= " ";
               let occurence= "";
             occurence =  length.numberofsequence;
              console.log(occurence);  */
            } } }}
let word = "ushaaa";
frequency();